# Claude Code 启动文案(复制即用)

请先完整阅读 design/HANDOFF.md,以及两份设计稿:
- design/盯盘Pro 足球盘口终端.dc.html(移动端,390px)
- design/足球终端 桌面版.dc.html(桌面端,≥1080px 三栏终端)
重点:移动版逻辑类顶部两段注释——【数据抓取分层计划】与【catalog.ts 39 端点对齐结论】。

约束(必须严格执行):
1. 设计稿是唯一视觉基准:像素、文案、配色 token、状态变体(打码/封盘/空状态/锁定/已解锁)照稿实现;
   两端共享 token 与数据契约,仅视图层分叉(≥1240 三栏,<1080 切移动布局)
2. 商业规则:免注册可浏览(列表前3场+直播完整,其余打码;异动前3条完整);
   注册(邮箱+密码,免验证)后全站数据免费;预测+AI报告是唯一收费项,
   赛前38/滚球58积分按场解锁、永久可见;新人58积分;邀请+1/人(日10/周30/月100);
   兑换码一次性;充值档位与加赠按稿
3. 数据层:基于现有 catalog.ts 的 runAfEndpoint,按注释实现统一调度器(按距开球分层,
   滚球与 T-30min 内 force=true 绕 10min TTL)、odds/predictions 快照持续归档
   (AF 赛前 odds 仅 1–14 天窗口)、急变阈值(盘口≥0.25 或水位|Δ|≥0.05)、
   matchPanorama(fixtureId) 聚合层
4. 已确认无端点支撑而删除的功能:裁判统计——不要自行加回;
   不得新增任何 39 端点之外的数据功能
5. 开发顺序:脚手架+token → 账户/积分/解锁/兑换/邀请(平台侧) → 数据层 →
   页面接真数据(列表→详情→异动→预测) → AI 报告生成(按报告页五分区结构) →
   分享海报/战绩 job/工单后台
6. 每完成一个模块停下,贴截图供验收;先跑 npm run af -- selftest 确认 39 端点连通

先输出实施计划(模块拆分+里程碑+每步验收标准),确认后再动手。
设计稿内 demo 数据(场次/积分数值/战绩)均为占位,以真实 API 与后端为准。
