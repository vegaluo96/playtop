# MiCall 后端依赖。
#
# 骨架的核心逻辑（config/protocol/session/context/memory/emotion/billing）零三方依赖，
# 单元测试（python3 -m tests）在任何环境直接跑。以下是「运行服务 / 接真实供应商」所需：

# 信令服务器（WebSocket）。骨架的 server 层用它。
websockets>=12.0

# Provider 真实 HTTP 调用（apiyi / 百炼 / MiniMax）。
httpx>=0.27

# Postgres 持久化（配了 database.dsn 才用；未装/未配则回退内存仓储）。
# pgvector 用裸 SQL 的 ::vector 转换，无需额外 python 包。
psycopg[binary,pool]>=3.1

# 服务端 WebRTC 媒体面（真全双工 = 通话中可打断 + 外放硬件 AEC，前端配了 coturn 即默认走它）。
# 从 wheel 直装，无需 apt 装 ffmpeg/opus（会带上 av / pylibsrtp / aioice 等自带原生库的 wheel）。
# 注意：这是「打断功能」的硬依赖——少了它后端发 rtc_unavailable，前端退 WS 半双工（AI 外放时静麦=不能打断）。
# 故由「可选」改为正式依赖，避免重部署后悄悄丢失、打断又坏。装：pip install aiortc
aiortc>=1.13

# —— 以下为真实接入时启用（骨架用 stub + 标准库，不强制安装）——
# pipecat-ai>=0.0.40     # 实时编排框架（WebRTC + VAD + ASR/LLM/TTS 流式串接 + 打断）
#   或 livekit-agents    # 备选实时编排框架
